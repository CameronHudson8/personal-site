apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "personal-site.fullname" . }}-test-connection"
  labels:
{{ include "personal-site.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "personal-site.fullname" . }}:{{ .Values.test.service.port }}']
  restartPolicy: Never

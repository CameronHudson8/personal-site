apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: reverse-proxy
  namespace: personal-site-testing
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: testing.cameronhudson8.com
    http:
      paths:
      - path: /
        backend:
          serviceName: backend
          servicePort: 80
      # - path: /api
      #   backend:
      #     serviceName: backend
      #     servicePort: 3000
      # - path: /
      #   backend:
      #     serviceName: frontend
      #     servicePort: 80
  - host: testing.cameronhudson.info
    http:
      paths:
      - path: /
        backend:
          serviceName: backend
          servicePort: 80
      # - path: /api
      #   backend:
      #     serviceName: backend
      #     servicePort: 3000
      # - path: /
      #   backend:
      #     serviceName: frontend
      #     servicePort: 80
